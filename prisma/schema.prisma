// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


model Author {
  id_author   Integer   @id @default(autoincrement())
  name        String
  first_name  String
  biography   String?
  nationality String?
  die_date    DateTime?
  birth_date  DateTime?
  books Book[]
}

model User {
  id_user    Integer   @id @default(autoincrement())
  name       String
  first_name String
  email      String
  password   String
  sign_date  DateTime @default(now())
  matricule  String
  role       Role    
  borrows    Borrow[]
   link_image_user String
}

model Category {
  id_category Integer   @id @default(autoincrement())
  type        String
  description String?
  books       Book[]
}

model Book {
  id_book     Integer   @id @default(autoincrement())
  title        String
  page         Int
  langage      String
  description  String?
  parution_date DateTime  @default(now())
  id_category String
  category Category @relation(fields: [id_category], references: [id_category])
  id_author  String
  author Author? @relation(fields: [id_author], references: [id_author])
    borrow Borrow[]
  link_image_book String
}

model Borrow {
  id_borrow Integer   @id @default(autoincrement())
  user User @relation(fields: [id_user], references: [id_user])
  book Book @relation(fields: [id_book], references: [id_book])
  id_user String
  id_book String
}

enum Role{
  USER
  ADMIN
}